- name: Installing packages
  yum: name={{ item }}
  with_items:
      - vim
      - wget
      - iftop
      - iotop
      - sysstat
      - hdparm
      - atop
      - lshw
      - tmux
      - python-pip

- name: copy hosts file
  copy: src="/etc/hosts" dest="/etc/hosts"
