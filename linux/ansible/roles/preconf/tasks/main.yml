- name: Installing packages
  yum: name={{ item }}
  with_items:
      - vim
      - wget
      - iftop
      - iotop
      - sysstat
      - atop
      - hdparm
      - lshw
      - tmux

- name: copy hosts file
  copy: src="/etc/hosts" dest="/etc/hosts"
